import { RoundedValuePipe } from './rounded-value.pipe';

describe('RoundedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
